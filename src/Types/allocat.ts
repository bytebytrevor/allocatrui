export type Allocat = {
    id: string;
    fullName: string;
    title: string
    skills: string[],
    rating: number,
    completedProjects: number,
    available: boolean,
    verified: boolean,
    location: string,
    hourlyRate: number,
    currency: string,
    joinedAt: string
}